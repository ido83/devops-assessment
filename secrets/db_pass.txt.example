# Copy this file to db_pass.txt and replace with a strong password.
# db_pass.txt is gitignored and must never be committed.
#
# Requirements: min 16 characters, mixed case, numbers, and symbols.
# Example generator: openssl rand -base64 24
REPLACE_WITH_STRONG_PASSWORD
